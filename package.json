{
  "name": "parcel-setup",
  "description": "Parcel sample setup",
  "version": "0.0.0",
  "license": "Unlicense",
  "private": true,
  "scripts": {
    "clean": "rm -rf .cache dist",
    "dev": "parcel index.html",
    "build": "parcel build index.html"
  },
  "devDependencies": {
    "autoprefixer": "^8.5.1",
    "parcel-bundler": "^1.8.1"
  }
}
